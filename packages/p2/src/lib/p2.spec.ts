import { p2, p2p1 } from '../';

describe('p2', () => {
  it('should work', () => {
    expect(p2()).toEqual('p2');
  });
  it('should work with p1', () => {
    expect(p2p1()).toEqual('p2p1');
  });
});
